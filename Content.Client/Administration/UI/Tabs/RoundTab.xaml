<Control
    xmlns="https://spacestation14.io"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    xmlns:ds="clr-namespace:Content.Client.DeadSpace.Administration.UI"
    Margin="4"
    MinSize="50 50">
    <BoxContainer Orientation="Vertical">
        <GridContainer
            Columns="2">
            <controls:ConfirmButton Name="StartRound" Text="{Loc administration-ui-round-tab-start-round}" />
            <controls:ConfirmButton Name="EndRound" Text="{Loc administration-ui-round-tab-end-round}" />
            <controls:ConfirmButton Name="RestartRound" Text="{Loc administration-ui-round-tab-restart-round}" />
            <controls:ConfirmButton Name="RestartRoundNow" Text="{Loc administration-ui-round-tab-restart-round-now}" />
        </GridContainer>
        <Button Name="CentCommTransferButton" Text="{Loc centcomm-transfer-title}" />

        <!-- DS14-start -->
        <CheckBox Name="SecondaryAnnouncementsToggle"
                  Text="{Loc administration-ui-round-tab-secondary-announcements}"
                  Visible="False" />
        <!-- DS14-end -->

        <ds:AutoMapVoteRoundControl Name="AutoMapVoteControl" />
    </BoxContainer>
</Control>
